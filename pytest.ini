[pytest]
# `pytest` を直接叩いてもリポジトリルートが import パスに入るようにする
# (これが無いと tests/conftest.py の `from src.providers.base import ...` が
#  ModuleNotFoundError になり、`python -m pytest` でしか通らなかった)
pythonpath = .
testpaths = tests
